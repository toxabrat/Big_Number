file(GLOB_RECURSE TESTS "*.cpp")
add_subdirectory(googletest)
include(GoogleTest)
add_executable(tests ${TESTS})
include_directories(..)
target_link_libraries(tests gtest gtest_main BigNumber)
enable_testing()
gtest_discover_tests(tests)